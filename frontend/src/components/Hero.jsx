import React from "react";

const Hero = () => (
  <section className="hero">
    <h2 className="hero-title">
      Discover Your Next
      <span className="hero-gradient"> Great Read</span>
    </h2>
    <p className="hero-subtitle">
      Explore curated collections of books across all genres. Find your perfect match today.
    </p>
  </section>
);

export default Hero;
